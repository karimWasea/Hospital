@{
    ViewData["Title"] = "Edit Shift";
    Layout = "_Layout";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, Cloudscribe.Web.Pagination
<input type="hidden" asp-for="id" />

@model hospitalVm.ApointmentVm

<h1>Mange Visite</h1>
<link href="~/css/appiontmentvisitsave.css" rel="stylesheet" />
<

<partial name="_AlertMessage" />
<div class="container mt-5 glass-container col-md-7">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <form asp-action="Save" method="post" id="saveform" enctype="multipart/form-data">

                <!-- Group related fields using div containers -->
                <input asp-for="doctorid" type="hidden" />
                <input asp-for="DoctorAppointmentVIsitid" type="hidden" />
                <input asp-for="doctorid" type="hidden" />
                <input asp-for="patientid" type="hidden" />
                <input asp-for="id" type="hidden" />

                <div class="form-row">
                    <div class="form-group glass-container">
                        <label asp-for="VisiteNotebydoctor">Doctor's Note:</label>
                        <textarea asp-for="VisiteNotebydoctor" class="form-control glass-input"></textarea>
                    </div>
                    <div class="form-group glass-container">
                        <label asp-for="discreaption">Visit Description:</label>
                        <textarea asp-for="discreaption" class="form-control glass-input"></textarea>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group glass-container">
                        <label asp-for="VisitType">Visit Type:</label>
                        <select asp-for="VisitType" class="form-control glass-select" asp-items="@ViewBag.Visitetype">
                            <option value="">Select Visit Type</option>
                            @foreach (var item in ViewBag.Visitetype)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                    <div class="form-group glass-container">
                        <label asp-for="VisitStatus">Visit Status:</label>
                        <select asp-for="VisitStatus" class="form-control glass-select" asp-items="@ViewBag.viststuts">
                            <option value="">Select Visit Status</option>
                            @foreach (var item in ViewBag.viststuts)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group glass-container">
                        <label asp-for="doctorid">Doctor Name:</label>
                        <select asp-for="doctorid" class="form-control glass-select" asp-items="@ViewBag.doctordam">
                            <option value="">Select Doctor</option>
                            @foreach (var item in ViewBag.doctordam)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                    <div class="form-group glass-container">
                        <label asp-for="patientid">Patient Name:</label>
                        <select asp-for="patientid" class="form-control glass-select" asp-items="@ViewBag.allpatient">
                            <option value="">Select Patient</option>
                            @foreach (var item in ViewBag.allpatient)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                </div>

                <div class="form-group glass-container">
                    <label asp-for="CreateDate">Appointment Time:</label>
                    <select asp-for="CreateDate" class="form-control glass-select">
                        <option value="">Select a time slot</option>
                        @foreach (var slot in ViewBag.AvailableSlots)
                        {
                            <option value="@slot.Value">@slot.Text</option>
                        }
                    </select>
                </div>

                <div class="button-group">
                    <button type="submit" id="savedata" class="btn btn-primary">
                        @if (Model != null && Model.id > 0)
                        {
                            <text>Edit</text>
                        }
                        else
                        {
                            <text>Add New</text>
                        }  </button>
                    <a asp-controller="Appiontmentvisite" asp-action="Index" data-action="edit" class="btn btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script><script src="~/js/appiontmentvisitesave.js"></script>
   
}
